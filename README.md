# xws_admin
Adminski modul

Pokretanje:

cd [Git Repository Path]\XwsAdmin
ng serve --proxy-config proxy.conf.json

proxy.conf.json je podesen da http requestove poslate
na /backend/* salje na localhost:8080/* pri cemu se brise
/backend
